# Store uploaded images and the SQLite metadata database on the Pi 5 SSD mount.
# Change this path to your actual SSD directory before starting the backend.
EDGE_MONITORING_DATA_DIR=/mnt/ssd/edge-monitoring-data

# Maximum accepted image size in bytes. Default: 5 MiB.
MAX_UPLOAD_BYTES=5242880
