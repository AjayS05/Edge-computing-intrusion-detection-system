{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"task1/architecture/","title":"Task 1: System Architecture &amp; Hardware Set-up","text":""},{"location":"task1/architecture/#overview","title":"Overview","text":"<p>The system consists of:</p> <ul> <li>Raspberry Pi sensor nodes</li> <li>AI inference layer</li> <li>Backend services</li> <li>Monitoring infrastructure</li> <li>Frontend dashboard</li> </ul>"},{"location":"task1/hardware/","title":"Hardware Components","text":""},{"location":"task1/hardware/#architecture-diagram-hardware-only","title":"Architecture Diagram - Hardware only","text":""},{"location":"task1/network/","title":"Raspberry Pi PXE / Network-Boot Setup","text":""},{"location":"task1/network/#overview","title":"Overview","text":"<p>This document describes the network-boot setup used for a Raspberry Pi cluster.</p> <p>The Raspberry Pi 5 acts as the central server for eight Raspberry Pi 3 Model B+ nodes. The Raspberry Pi 3 nodes boot over Ethernet instead of relying on a full operating system stored locally on individual SD cards.</p> <p>The setup uses:</p> <ul> <li>DHCP to assign fixed IP addresses to each Raspberry Pi 3 node</li> <li>TFTP to provide boot files</li> <li>NFS to provide a writable Linux root filesystem for each node</li> <li>External SSD storage connected to the Raspberry Pi 5 for storing the node operating-system folders</li> </ul>"},{"location":"task1/network/#cluster-architecture","title":"Cluster Architecture","text":"<pre><code>                         Ethernet Switch\n                               |\n        -------------------------------------------------\n        |       |       |       |       |       |       |\n      RPi5   RPi3-01 RPi3-02 RPi3-03   ...   RPi3-07 RPi3-08\n     Server\n</code></pre> <p>The Raspberry Pi 5 provides:</p> <pre><code>DHCP server   \u2192 dnsmasq\nTFTP server   \u2192 dnsmasq\nNFS server    \u2192 nfs-kernel-server\nNFS storage   \u2192 external SSD mounted at /srv/nfs\n</code></pre>"},{"location":"task1/network/#ip-address-plan","title":"IP Address Plan","text":"Device IP Address Purpose Raspberry Pi 5 <code>192.168.50.1</code> DHCP, TFTP and NFS server Raspberry Pi 3-01 <code>192.168.50.101</code> Network-boot client Raspberry Pi 3-02 <code>192.168.50.102</code> Network-boot client Raspberry Pi 3-03 <code>192.168.50.103</code> Network-boot client Raspberry Pi 3-04 <code>192.168.50.104</code> Network-boot client Raspberry Pi 3-05 <code>192.168.50.105</code> Network-boot client Raspberry Pi 3-06 <code>192.168.50.106</code> Network-boot client Raspberry Pi 3-07 <code>192.168.50.107</code> Network-boot client Raspberry Pi 3-08 <code>192.168.50.108</code> Network-boot client <p>Each Raspberry Pi 3 node receives its fixed IP address from the Raspberry Pi 5 based on its Ethernet MAC address.</p>"},{"location":"task1/network/#boot-flow","title":"Boot Flow","text":"<pre><code>RPi3 powers on\n   \u2193\nRequests an IP address using DHCP\n   \u2193\nRPi5 assigns a fixed IP address using dnsmasq\n   \u2193\nRPi3 downloads boot files from the RPi5 TFTP server\n   \u2193\nRPi3 mounts its own writable Linux root filesystem from RPi5 over NFS\n   \u2193\nRPi3 completes the operating-system boot process\n</code></pre>"},{"location":"task1/network/#1-install-required-packages-on-raspberry-pi-5","title":"1. Install Required Packages on Raspberry Pi 5","text":"<p>Run:</p> <pre><code>sudo apt update\nsudo apt install dnsmasq nfs-kernel-server rsync tcpdump -y\n</code></pre> <p>The packages are used for:</p> Package Purpose <code>dnsmasq</code> DHCP and TFTP services <code>nfs-kernel-server</code> NFS root-filesystem sharing <code>rsync</code> Copying boot and operating-system files <code>tcpdump</code> DHCP and network-boot troubleshooting"},{"location":"task1/network/#2-configure-the-raspberry-pi-5-ethernet-address","title":"2. Configure the Raspberry Pi 5 Ethernet Address","text":"<p>The Raspberry Pi 5 Ethernet interface is configured with:</p> <pre><code>192.168.50.1/24\n</code></pre> <p>The static address is assigned to <code>eth0</code>.</p> <p>The address can be verified with:</p> <pre><code>ip addr show eth0\n</code></pre> <p>Expected output includes:</p> <pre><code>inet 192.168.50.1/24\n</code></pre>"},{"location":"task1/network/#3-collect-raspberry-pi-3-mac-addresses","title":"3. Collect Raspberry Pi 3 MAC Addresses","text":"<p>Each Raspberry Pi 3 was booted normally from an SD card first.</p> <p>The Ethernet MAC address was collected using:</p> <pre><code>cat /sys/class/net/eth0/address\n</code></pre> <p>The model was checked using:</p> <pre><code>cat /proc/device-tree/model\n</code></pre> <p>The nodes used in this setup are Raspberry Pi 3 Model B+ boards.</p> <p>A private record should be maintained separately:</p> Node Ethernet MAC Address Fixed IP <code>rpi3-01</code> <code>&lt;MAC_ADDRESS_PI1&gt;</code> <code>192.168.50.101</code> <code>rpi3-02</code> <code>&lt;MAC_ADDRESS_PI2&gt;</code> <code>192.168.50.102</code> <code>rpi3-03</code> <code>&lt;MAC_ADDRESS_PI3&gt;</code> <code>192.168.50.103</code> <code>rpi3-04</code> <code>&lt;MAC_ADDRESS_PI4&gt;</code> <code>192.168.50.104</code> <code>rpi3-05</code> <code>&lt;MAC_ADDRESS_PI5&gt;</code> <code>192.168.50.105</code> <code>rpi3-06</code> <code>&lt;MAC_ADDRESS_PI6&gt;</code> <code>192.168.50.106</code> <code>rpi3-07</code> <code>&lt;MAC_ADDRESS_PI7&gt;</code> <code>192.168.50.107</code> <code>rpi3-08</code> <code>&lt;MAC_ADDRESS_PI8&gt;</code> <code>192.168.50.108</code> <p>Do not publish real MAC addresses in a public repository unless required.</p>"},{"location":"task1/network/#4-configure-dnsmasq-for-dhcp-and-tftp","title":"4. Configure dnsmasq for DHCP and TFTP","text":"<p>The configuration file is:</p> <pre><code>/etc/dnsmasq.conf\n</code></pre> <p>The following configuration was added:</p> <pre><code>interface=eth0\nbind-interfaces\ndhcp-authoritative\n\n# DHCP range\ndhcp-range=192.168.50.101,192.168.50.150,255.255.255.0,24h\n\n# Raspberry Pi 5 as gateway and DNS server\ndhcp-option=3,192.168.50.1\ndhcp-option=6,192.168.50.1\ndhcp-option=66,192.168.50.1\n\n# TFTP configuration\nenable-tftp\ntftp-root=/srv/tftp\ndhcp-boot=bootcode.bin\n\n# Debug logging\nlog-dhcp\nlog-queries\n\n# Fixed DHCP assignments\ndhcp-host=&lt;MAC_ADDRESS_PI1&gt;,192.168.50.101,rpi3-01\ndhcp-host=&lt;MAC_ADDRESS_PI2&gt;,192.168.50.102,rpi3-02\ndhcp-host=&lt;MAC_ADDRESS_PI3&gt;,192.168.50.103,rpi3-03\ndhcp-host=&lt;MAC_ADDRESS_PI4&gt;,192.168.50.104,rpi3-04\ndhcp-host=&lt;MAC_ADDRESS_PI5&gt;,192.168.50.105,rpi3-05\ndhcp-host=&lt;MAC_ADDRESS_PI6&gt;,192.168.50.106,rpi3-06\ndhcp-host=&lt;MAC_ADDRESS_PI7&gt;,192.168.50.107,rpi3-07\ndhcp-host=&lt;MAC_ADDRESS_PI8&gt;,192.168.50.108,rpi3-08\n</code></pre> <p>Restart and verify dnsmasq:</p> <pre><code>sudo systemctl restart dnsmasq\nsudo systemctl status dnsmasq\n</code></pre> <p>DHCP activity can be monitored using:</p> <pre><code>sudo journalctl -u dnsmasq -f\n</code></pre> <p>The DHCP lease file can be checked using:</p> <pre><code>cat /var/lib/misc/dnsmasq.leases\n</code></pre> <p>Expected fixed assignments:</p> <pre><code>rpi3-01 \u2192 192.168.50.101\nrpi3-02 \u2192 192.168.50.102\nrpi3-03 \u2192 192.168.50.103\nrpi3-04 \u2192 192.168.50.104\nrpi3-05 \u2192 192.168.50.105\nrpi3-06 \u2192 192.168.50.106\nrpi3-07 \u2192 192.168.50.107\nrpi3-08 \u2192 192.168.50.108\n</code></pre>"},{"location":"task1/network/#5-create-tftp-and-nfs-directories","title":"5. Create TFTP and NFS Directories","text":"<p>Create a boot folder and root-filesystem folder for each node:</p> <pre><code>sudo mkdir -p /srv/tftp\nsudo mkdir -p /srv/nfs\n\nfor i in 01 02 03 04 05 06 07 08; do\n  sudo mkdir -p /srv/tftp/rpi3-$i\n  sudo mkdir -p /srv/nfs/rpi3-$i\ndone\n</code></pre> <p>Folder structure:</p> <pre><code>/srv/tftp/\n\u251c\u2500\u2500 rpi3-01\n\u251c\u2500\u2500 rpi3-02\n\u251c\u2500\u2500 ...\n\u2514\u2500\u2500 rpi3-08\n\n/srv/nfs/\n\u251c\u2500\u2500 rpi3-01\n\u251c\u2500\u2500 rpi3-02\n\u251c\u2500\u2500 ...\n\u2514\u2500\u2500 rpi3-08\n</code></pre> <p>Each node uses a separate writable NFS root filesystem. This avoids conflicts involving hostnames, SSH keys, package-manager locks, logs and service state.</p>"},{"location":"task1/network/#6-prepare-the-base-raspberry-pi-os-files","title":"6. Prepare the Base Raspberry Pi OS Files","text":"<p>A Raspberry Pi OS SD card was inserted into the Raspberry Pi 5 using a USB card reader.</p> <p>The partitions were identified using:</p> <pre><code>lsblk -f\n</code></pre> <p>Example:</p> <pre><code>sda\n\u251c\u2500sda1  vfat  bootfs\n\u2514\u2500sda2  ext4  rootfs\n</code></pre> <p>Mount the root filesystem:</p> <pre><code>sudo mkdir -p /mnt/rpi3-root\nsudo mount /dev/sda2 /mnt/rpi3-root\n</code></pre> <p>Copy boot files:</p> <pre><code>sudo rsync -a /media/pi5/bootfs/ /srv/tftp/rpi3-01/\n</code></pre> <p>Copy the root filesystem:</p> <pre><code>sudo rsync -aAXv /mnt/rpi3-root/ /srv/nfs/rpi3-01/\n</code></pre> <p>Verify files:</p> <pre><code>ls /srv/tftp/rpi3-01\nls /srv/nfs/rpi3-01\n</code></pre> <p>The TFTP folder should include files such as:</p> <pre><code>bootcode.bin\ncmdline.txt\nconfig.txt\nstart.elf\nfixup.dat\nkernel8.img\nbcm2710-rpi-3-b-plus.dtb\noverlays/\n</code></pre> <p>The NFS root should include directories such as:</p> <pre><code>bin\nboot\ndev\netc\nhome\nlib\nproc\nroot\nrun\nsbin\nsys\ntmp\nusr\nvar\n</code></pre>"},{"location":"task1/network/#7-move-nfs-storage-to-an-external-ssd","title":"7. Move NFS Storage to an External SSD","text":"<p>A dedicated external SSD was connected to the Raspberry Pi 5 because storing eight writable operating-system copies on the Pi 5 SD card required too much space.</p> <p>The SSD was identified as:</p> <pre><code>/dev/sdb\n</code></pre> <p>A GPT partition and an <code>ext4</code> filesystem were created:</p> <pre><code>sudo fdisk /dev/sdb\n</code></pre> <p>Inside <code>fdisk</code>:</p> <pre><code>g\nn\n1\nEnter\nEnter\nw\n</code></pre> <p>Format the partition:</p> <pre><code>sudo mkfs.ext4 -L rpi-nfs-storage /dev/sdb1\n</code></pre> <p>Mount temporarily:</p> <pre><code>sudo mkdir -p /mnt/external-nfs\nsudo mount /dev/sdb1 /mnt/external-nfs\n</code></pre> <p>Copy existing NFS content:</p> <pre><code>sudo rsync -aAXv /srv/nfs/ /mnt/external-nfs/\n</code></pre> <p>Replace <code>/srv/nfs</code> with the external SSD mount:</p> <pre><code>sudo mv /srv/nfs /srv/nfs-old\nsudo mkdir -p /srv/nfs\nsudo umount /mnt/external-nfs\nsudo mount /dev/sdb1 /srv/nfs\n</code></pre> <p>Verify:</p> <pre><code>df -h /srv/nfs\nls /srv/nfs\n</code></pre> <p>Expected result:</p> <pre><code>/dev/sdb1 ... /srv/nfs\n</code></pre> <p>The SSD UUID was retrieved using:</p> <pre><code>sudo blkid /dev/sdb1\n</code></pre> <p>The following line was added to <code>/etc/fstab</code>:</p> <pre><code>UUID=&lt;UUID_OF_EXTERNAL_SSD&gt; /srv/nfs ext4 defaults,noatime 0 2\n</code></pre> <p>Reload and test:</p> <pre><code>sudo systemctl daemon-reload\nsudo umount /srv/nfs\nsudo mount -a\ndf -h /srv/nfs\n</code></pre> <p>After confirming the SSD mount, the old SD-card copy was removed:</p> <pre><code>sudo rm -rf /srv/nfs-old\n</code></pre>"},{"location":"task1/network/#8-duplicate-the-root-filesystem-for-remaining-nodes","title":"8. Duplicate the Root Filesystem for Remaining Nodes","text":"<p>Copy the base root filesystem to the remaining nodes:</p> <pre><code>for i in 02 03 04 05 06 07 08; do\n  sudo rsync -aAXv --delete /srv/nfs/rpi3-01/ /srv/nfs/rpi3-$i/\ndone\n</code></pre> <p>Copy TFTP boot files:</p> <pre><code>for i in 02 03 04 05 06 07 08; do\n  sudo rsync -a /srv/tftp/rpi3-01/ /srv/tftp/rpi3-$i/\ndone\n</code></pre> <p>Check NFS folder sizes:</p> <pre><code>sudo du -sh /srv/nfs/rpi3-*\n</code></pre> <p>All folders should have approximately the same size.</p>"},{"location":"task1/network/#9-configure-nfs-exports","title":"9. Configure NFS Exports","text":"<p>The NFS exports file is:</p> <pre><code>/etc/exports\n</code></pre> <p>For testing, the full cluster subnet may be allowed:</p> <pre><code>/srv/nfs/rpi3-01 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-02 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-03 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-04 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-05 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-06 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-07 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-08 192.168.50.0/24(rw,sync,no_subtree_check,no_root_squash)\n</code></pre> <p>Reload exports:</p> <pre><code>sudo exportfs -ra\nsudo systemctl restart nfs-kernel-server\nsudo exportfs -v\nshowmount -e localhost\n</code></pre> <p>For a stricter final configuration, each NFS folder can be restricted to its assigned node IP:</p> <pre><code>/srv/nfs/rpi3-01 192.168.50.101(rw,sync,no_subtree_check,no_root_squash)\n/srv/nfs/rpi3-02 192.168.50.102(rw,sync,no_subtree_check,no_root_squash)\n...\n</code></pre>"},{"location":"task1/network/#10-configure-nfs-root-boot-commands","title":"10. Configure NFS Root Boot Commands","text":"<p>Each Raspberry Pi 3 boot folder requires its own <code>cmdline.txt</code>.</p> <p>Run:</p> <pre><code>for i in 01 02 03 04 05 06 07 08; do\n  echo \"console=serial0,115200 console=tty1 root=/dev/nfs nfsroot=192.168.50.1:/srv/nfs/rpi3-$i,vers=3 rw ip=dhcp rootwait\" | sudo tee /srv/tftp/rpi3-$i/cmdline.txt\ndone\n</code></pre> <p>Example for <code>rpi3-02</code>:</p> <pre><code>console=serial0,115200 console=tty1 root=/dev/nfs nfsroot=192.168.50.1:/srv/nfs/rpi3-02,vers=3 rw ip=dhcp rootwait\n</code></pre> <p><code>cmdline.txt</code> must remain a single line.</p>"},{"location":"task1/network/#11-set-unique-hostnames","title":"11. Set Unique Hostnames","text":"<p>Each node requires a unique hostname.</p> <p>Run:</p> <pre><code>for i in 01 02 03 04 05 06 07 08; do\n  echo \"rpi3-$i\" | sudo tee /srv/nfs/rpi3-$i/etc/hostname\n  sudo sed -i \"s/^127.0.1.1.*/127.0.1.1 rpi3-$i/\" /srv/nfs/rpi3-$i/etc/hosts\ndone\n</code></pre> <p>Expected hostnames:</p> <pre><code>rpi3-01\nrpi3-02\nrpi3-03\nrpi3-04\nrpi3-05\nrpi3-06\nrpi3-07\nrpi3-08\n</code></pre>"},{"location":"task1/network/#12-test-dhcp-and-network-boot","title":"12. Test DHCP and Network Boot","text":"<p>Monitor dnsmasq logs:</p> <pre><code>sudo journalctl -u dnsmasq -f\n</code></pre> <p>Power on one Raspberry Pi 3B+ at a time without a full OS SD card.</p> <p>Expected DHCP activity:</p> <pre><code>DHCPDISCOVER\nDHCPOFFER\nDHCPREQUEST\nDHCPACK\n</code></pre> <p>Check leases:</p> <pre><code>cat /var/lib/misc/dnsmasq.leases\n</code></pre> <p>Test connectivity:</p> <pre><code>ping 192.168.50.101\nping 192.168.50.102\n</code></pre> <p>Test SSH:</p> <pre><code>ssh &lt;USERNAME&gt;@192.168.50.101\n</code></pre>"},{"location":"task1/network/#13-troubleshooting","title":"13. Troubleshooting","text":""},{"location":"task1/network/#dhcp-offer-appears-but-no-dhcp-acknowledgement","title":"DHCP Offer Appears but No DHCP Acknowledgement","text":"<p>Verify:</p> <pre><code>sudo journalctl -u dnsmasq -f\n</code></pre> <p>Check that <code>dnsmasq.conf</code> contains:</p> <pre><code>dhcp-authoritative\ndhcp-option=66,192.168.50.1\ndhcp-boot=bootcode.bin\n</code></pre>"},{"location":"task1/network/#nfs-mount-returns-permission-denied","title":"NFS Mount Returns Permission Denied","text":"<p>Check actual DHCP leases:</p> <pre><code>cat /var/lib/misc/dnsmasq.leases\n</code></pre> <p>Check exports:</p> <pre><code>sudo exportfs -v\n</code></pre> <p>Reload exports:</p> <pre><code>sudo exportfs -ra\nsudo systemctl restart nfs-kernel-server\n</code></pre> <p>For diagnosis, temporarily allow the whole subnet:</p> <pre><code>192.168.50.0/24\n</code></pre>"},{"location":"task1/network/#nfs-server-fails-to-restart","title":"NFS Server Fails to Restart","text":"<p>Check:</p> <pre><code>sudo systemctl status nfs-kernel-server\nsudo journalctl -xeu nfs-server.service\n</code></pre> <p>Verify external SSD mount:</p> <pre><code>df -h /srv/nfs\n</code></pre> <p>Expected:</p> <pre><code>/dev/sdb1 ... /srv/nfs\n</code></pre>"},{"location":"task1/network/#no-space-left-on-device","title":"No Space Left on Device","text":"<p>Check:</p> <pre><code>df -h /\ndf -h /srv/nfs\n</code></pre> <p>The NFS root folders should be stored on the external SSD mounted at:</p> <pre><code>/srv/nfs\n</code></pre>"},{"location":"task1/network/#verify-tftp-boot-files","title":"Verify TFTP Boot Files","text":"<pre><code>ls /srv/tftp/rpi3-01\n</code></pre> <p>Expected files include:</p> <pre><code>bootcode.bin\ncmdline.txt\nconfig.txt\nstart.elf\nfixup.dat\nkernel8.img\noverlays/\n</code></pre>"},{"location":"task1/network/#current-status","title":"Current Status","text":"<p>Completed:</p> <ul> <li>Raspberry Pi 5 static Ethernet address configured as <code>192.168.50.1</code></li> <li><code>dnsmasq</code> configured for DHCP and TFTP</li> <li>Fixed IP addresses assigned to all eight Raspberry Pi 3 nodes</li> <li>Raspberry Pi OS boot and root files copied</li> <li>External SSD mounted at <code>/srv/nfs</code></li> <li>Separate writable NFS root folders created for all eight Raspberry Pi 3 nodes</li> <li>Initial network-boot test completed successfully for <code>rpi3-01</code></li> </ul> <p>Remaining validation:</p> <ul> <li>Confirm NFS service restart after final export changes</li> <li>Confirm TFTP folder selection for each Raspberry Pi 3 node</li> <li>Validate network boot for <code>rpi3-02</code> to <code>rpi3-08</code></li> <li>Restrict NFS exports from full subnet access to node-specific IP addresses after testing</li> </ul>"},{"location":"task1/network/#notes","title":"Notes","text":"<p>Raspberry Pi 3 Model B+ boards support Ethernet network boot without requiring a custom BIOS-style boot-order configuration.</p> <p>Each node uses a separate writable NFS root filesystem to avoid conflicts involving:</p> <ul> <li>Hostnames</li> <li>SSH host keys</li> <li>Linux logs</li> <li>Package-manager locks</li> <li>Service state</li> <li>Runtime files</li> </ul>"},{"location":"task2/hpl/","title":"HPL & GFLOPS Benchmarks","text":""},{"location":"task2/hpl/#hpl-gflops","title":"HPL, GFLOPS","text":""}]}